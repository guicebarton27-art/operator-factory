services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: autopilot
      POSTGRES_PASSWORD: autopilot
      POSTGRES_DB: autopilot
    ports:
      - "5432:5432"
    volumes:
      - autopilot_pgdata:/var/lib/postgresql/data
volumes:
  autopilot_pgdata:
